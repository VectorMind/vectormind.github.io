# Local tooling

This folder is reserved for the repository's local CLI, evidence-processing
automation, validation, and report generation. Collected inputs and generated
outputs belong under `.cache/`, not here.

The implementation language and packaging model are intentionally undecided
until a concrete automation requirement is planned.
