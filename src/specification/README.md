# Source specifications

This folder contains durable, implementation-independent contracts for tooling
under `src/`.

A specification should define inputs, outputs, provenance behavior, failure
modes, deterministic or reproducibility requirements, privacy boundaries, and
acceptance criteria. It should not contain dated implementation progress.
