# Verification

## Checks

- Confirm required repository entry-point files exist.
- Confirm Git ignores files created anywhere below `.cache/`.
- Confirm every row in `plans/open.md` and `plans/closed.md` resolves to a plan.
- Review the workflow documents for consistent path and lifecycle semantics.
